{% from '@macros/production.twig' import label %}
{% import '@macros/section.twig' as section %}

{{ section.start(gray_background, first, last) }}
    <h2>Animations</h2>
    <div class="w-64 flex flex-col mt-8">
        {% set items = [
            'Apples',
            'Bananas',
            'Cashews',
            'Spinach',
            'Zucchini',
        ] %}
        {% for item in items %}
            <h5 class="py-6 border-b-2">{{ item }}</h5>
        {% endfor %}
    </div>
{{ section.end() }}
